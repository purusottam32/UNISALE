// src/AuthLoader.jsx
import React from "react";

const AuthLoader = ({ children }) => {
  // Appwrite removed; AuthLoader is a no-op wrapper now
  return <>{children}</>;
};

export default AuthLoader;
